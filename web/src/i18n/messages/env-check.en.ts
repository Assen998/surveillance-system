export default {
  envCheck: {
    emptyHint: 'Click "Run Environment Check" at the top right to start',
    criticalFail: 'Critical environment checks failed ({n} failed): fix them per the instructions below before continuing',
    warnHint: 'Environment checks have warnings ({n}): the system can run, but address them as suggested',
    allPassed: 'All environment checks passed',
    checkItem: 'Check Item',
    criticalTag: 'Critical',
  },
}
